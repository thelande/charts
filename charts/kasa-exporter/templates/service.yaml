apiVersion: v1
kind: Service
metadata:
  name: {{ include "kasa-exporter.fullname" . }}
  labels:
    {{- include "kasa-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kasa-exporter.selectorLabels" . | nindent 4 }}
