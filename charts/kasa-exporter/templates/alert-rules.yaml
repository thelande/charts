{{- if .Values.prometheusRule.enabled -}}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "kasa-exporter.fullname" . }}
  labels:
    {{- include "kasa-exporter.labels" . | nindent 4 }}
spec:
  {{ .Files.Get "alert-rules.yaml" }}
{{- end }}
