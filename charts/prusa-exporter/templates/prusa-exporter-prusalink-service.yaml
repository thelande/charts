{{- /*
Copyright Thomas Helander. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

apiVersion: v1
kind: Service
metadata:
  name: {{ template "prusa-exporter.prusaExporter.fullname" . }}-prusa-exporter-prusalink
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: prusa-exporter
  {{- if or .Values.services.prusaExporterPrusalink.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" (dict "values" (list .Values.services.prusaExporterPrusalink.annotations .Values.commonAnnotations) "context" .) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{ $type := .Values.services.prusaExporterPrusalink.type }}
  type: {{ $type }}
  {{- if and .Values.services.prusaExporterPrusalink.clusterIP (eq $type "ClusterIP") }}
  clusterIP: {{ .Values.services.prusaExporterPrusalink.clusterIP }}
  {{- end }}
  {{- if .Values.services.prusaExporterPrusalink.sessionAffinity }}
  sessionAffinity: {{ .Values.services.prusaExporterPrusalink.sessionAffinity }}
  {{- end }}
  {{- if .Values.services.prusaExporterPrusalink.sessionAffinityConfig }}
  sessionAffinityConfig: {{- include "common.tplvalues.render" (dict "value" .Values.services.prusaExporterPrusalink.sessionAffinityConfig "context" $) | nindent 4 }}
  {{- end }}
  {{- if or (eq $type "LoadBalancer") (eq $type "NodePort") }}
  externalTrafficPolicy: {{ .Values.services.prusaExporterPrusalink.externalTrafficPolicy | quote }}
  {{- end }}
  {{- if and (eq $type "LoadBalancer") (not (empty .Values.services.prusaExporterPrusalink.loadBalancerSourceRanges)) }}
  loadBalancerSourceRanges: {{ .Values.services.prusaExporterPrusalink.loadBalancerSourceRanges }}
  {{- end }}
  {{- if and (eq $type "LoadBalancer") (not (empty .Values.services.prusaExporterPrusalink.loadBalancerIP)) }}
  loadBalancerIP: {{ .Values.services.prusaExporterPrusalink.loadBalancerIP }}
  {{- end }}
  ports:
    {{- range .Values.services.prusaExporterPrusalink.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      {{- if .targetPort }}
      targetPort: {{ .targetPort }}
      {{- end }}
      protocol: {{ .protocol }}
      {{- if .nodePort }}
      nodePort: {{ .nodePort }}
      {{- else if eq $type "ClusterIP" }}
      nodePort: null
      {{- end }}
    {{- end }}
    {{- if .Values.services.prusaExporterPrusalink.extraPorts }}
    {{- include "common.tplvalues.render" (dict "value" .Values.services.prusaExporterPrusalink.extraPorts "context" $) | nindent 4 }}
    {{- end }}
  {{- $podLabels := include "common.tplvalues.merge" (dict "values" (list .Values.prusaExporter.podLabels .Values.commonLabels) "context" .) | fromYaml }}
  selector: {{- include "common.labels.matchLabels" ( dict "customLabels" $podLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: prusa-exporter
