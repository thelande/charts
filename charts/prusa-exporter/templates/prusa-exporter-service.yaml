{{- /*
Copyright Thomas Helander. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

apiVersion: v1
kind: Service
metadata:
  name: {{ template "prusa-exporter.prusaExporter.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: prusa-exporter
  {{- if or .Values.services.prusaExporter.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" (dict "values" (list .Values.services.prusaExporter.annotations .Values.commonAnnotations) "context" .) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{ $type := .Values.services.prusaExporter.type }}
  type: {{ $type }}
  {{- if and .Values.services.prusaExporter.clusterIP (eq $type "ClusterIP") }}
  clusterIP: {{ .Values.services.prusaExporter.clusterIP }}
  {{- end }}
  {{- if .Values.services.prusaExporter.sessionAffinity }}
  sessionAffinity: {{ .Values.services.prusaExporter.sessionAffinity }}
  {{- end }}
  {{- if .Values.services.prusaExporter.sessionAffinityConfig }}
  sessionAffinityConfig: {{- include "common.tplvalues.render" (dict "value" .Values.services.prusaExporter.sessionAffinityConfig "context" $) | nindent 4 }}
  {{- end }}
  {{- if or (eq $type "LoadBalancer") (eq $type "NodePort") }}
  externalTrafficPolicy: {{ .Values.services.prusaExporter.externalTrafficPolicy | quote }}
  {{- end }}
  {{- if and (eq $type "LoadBalancer") (not (empty .Values.services.prusaExporter.loadBalancerSourceRanges)) }}
  loadBalancerSourceRanges: {{ .Values.services.prusaExporter.loadBalancerSourceRanges }}
  {{- end }}
  {{- if and (eq $type "LoadBalancer") (not (empty .Values.services.prusaExporter.loadBalancerIP)) }}
  loadBalancerIP: {{ .Values.services.prusaExporter.loadBalancerIP }}
  {{- end }}
  ports:
    {{- range .Values.services.prusaExporter.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      {{- if .targetPort }}
      targetPort: {{ .targetPort }}
      {{- end }}
      protocol: {{ .protocol }}
      {{- if .nodePort }}
      nodePort: {{ .nodePort }}
      {{- else if eq $type "ClusterIP" }}
      nodePort: null
      {{- end }}
    {{- end }}
    {{- if .Values.services.prusaExporter.extraPorts }}
    {{- include "common.tplvalues.render" (dict "value" .Values.services.prusaExporter.extraPorts "context" $) | nindent 4 }}
    {{- end }}
  {{- $podLabels := include "common.tplvalues.merge" (dict "values" (list .Values.prusaExporter.podLabels .Values.commonLabels) "context" .) | fromYaml }}
  selector: {{- include "common.labels.matchLabels" ( dict "customLabels" $podLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: prusa-exporter
