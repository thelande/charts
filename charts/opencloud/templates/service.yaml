{{- /*
Copyright Thomas Helander. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

apiVersion: v1
kind: Service
metadata:
  name: {{ template "opencloud.opencloud.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: opencloud
  {{- if or .Values.services.opencloud.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" (dict "values" (list .Values.services.opencloud.annotations .Values.commonAnnotations) "context" .) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{ $type := .Values.services.opencloud.type }}
  type: {{ $type }}
  {{- if and .Values.services.opencloud.clusterIP (eq $type "ClusterIP") }}
  clusterIP: {{ .Values.services.opencloud.clusterIP }}
  {{- end }}
  {{- if .Values.services.opencloud.sessionAffinity }}
  sessionAffinity: {{ .Values.services.opencloud.sessionAffinity }}
  {{- end }}
  {{- if .Values.services.opencloud.sessionAffinityConfig }}
  sessionAffinityConfig: {{- include "common.tplvalues.render" (dict "value" .Values.services.opencloud.sessionAffinityConfig "context" $) | nindent 4 }}
  {{- end }}
  {{- if or (eq $type "LoadBalancer") (eq $type "NodePort") }}
  externalTrafficPolicy: {{ .Values.services.opencloud.externalTrafficPolicy | quote }}
  {{- end }}
  {{- if and (eq $type "LoadBalancer") (not (empty .Values.services.opencloud.loadBalancerSourceRanges)) }}
  loadBalancerSourceRanges: {{ .Values.services.opencloud.loadBalancerSourceRanges }}
  {{- end }}
  {{- if and (eq $type "LoadBalancer") (not (empty .Values.services.opencloud.loadBalancerIP)) }}
  loadBalancerIP: {{ .Values.services.opencloud.loadBalancerIP }}
  {{- end }}
  ports:
    {{- range .Values.services.opencloud.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      {{- if .targetPort }}
      targetPort: {{ .targetPort }}
      {{- end }}
      protocol: {{ .protocol }}
      {{- if .nodePort }}
      nodePort: {{ .nodePort }}
      {{- else if eq $type "ClusterIP" }}
      nodePort: null
      {{- end }}
    {{- end }}
    {{- if .Values.services.opencloud.extraPorts }}
    {{- include "common.tplvalues.render" (dict "value" .Values.services.opencloud.extraPorts "context" $) | nindent 4 }}
    {{- end }}
  {{- $podLabels := include "common.tplvalues.merge" (dict "values" (list .Values.opencloud.podLabels .Values.commonLabels) "context" .) | fromYaml }}
  selector: {{- include "common.labels.matchLabels" ( dict "customLabels" $podLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: opencloud
