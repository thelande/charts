{{- /*
Copyright Thomas Helander. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

apiVersion: v1
kind: Service
metadata:
  name: {{ template "bar-assistant.salt-rim.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: salt-rim
  {{- if or .Values.services.saltRim.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" (dict "values" (list .Values.services.saltRim.annotations .Values.commonAnnotations) "context" .) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{ $type := .Values.services.saltRim.type }}
  type: {{ $type }}
  {{- if and .Values.services.saltRim.clusterIP (eq $type "ClusterIP") }}
  clusterIP: {{ .Values.services.saltRim.clusterIP }}
  {{- end }}
  {{- if .Values.services.saltRim.sessionAffinity }}
  sessionAffinity: {{ .Values.services.saltRim.sessionAffinity }}
  {{- end }}
  {{- if .Values.services.saltRim.sessionAffinityConfig }}
  sessionAffinityConfig: {{- include "common.tplvalues.render" (dict "value" .Values.services.saltRim.sessionAffinityConfig "context" $) | nindent 4 }}
  {{- end }}
  {{- if or (eq $type "LoadBalancer") (eq $type "NodePort") }}
  externalTrafficPolicy: {{ .Values.services.saltRim.externalTrafficPolicy | quote }}
  {{- end }}
  {{- if and (eq $type "LoadBalancer") (not (empty .Values.services.saltRim.loadBalancerSourceRanges)) }}
  loadBalancerSourceRanges: {{ .Values.services.saltRim.loadBalancerSourceRanges }}
  {{- end }}
  {{- if and (eq $type "LoadBalancer") (not (empty .Values.services.saltRim.loadBalancerIP)) }}
  loadBalancerIP: {{ .Values.services.saltRim.loadBalancerIP }}
  {{- end }}
  ports:
    {{- range .Values.services.saltRim.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      {{- if .targetPort }}
      targetPort: {{ .targetPort }}
      {{- end }}
      protocol: {{ .protocol }}
      {{- if .nodePort }}
      nodePort: {{ .nodePort }}
      {{- else if eq $type "ClusterIP" }}
      nodePort: null
      {{- end }}
    {{- end }}
    {{- if .Values.services.saltRim.extraPorts }}
    {{- include "common.tplvalues.render" (dict "value" .Values.services.saltRim.extraPorts "context" $) | nindent 4 }}
    {{- end }}
  {{- $podLabels := include "common.tplvalues.merge" (dict "values" (list .Values.saltRim.podLabels .Values.commonLabels) "context" .) | fromYaml }}
  selector: {{- include "common.labels.matchLabels" ( dict "customLabels" $podLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: salt-rim
