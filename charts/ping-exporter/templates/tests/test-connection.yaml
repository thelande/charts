apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ping-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "ping-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ping-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
