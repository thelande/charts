apiVersion: v1
kind: Service
metadata:
  name: {{ include "ping-exporter.fullname" . }}
  labels:
    {{- include "ping-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ping-exporter.selectorLabels" . | nindent 4 }}
