{{- if .Values.serviceMonitor.enabled -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "ping-exporter.fullname" . }}
  labels:
    {{- include "ping-exporter.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "ping-exporter.selectorLabels" . | nindent 6 }}
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  endpoints:
    - port: http
      interval: {{ .Values.serviceMonitor.scrapeInterval }}
{{- end }}
