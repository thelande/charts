{{- /*
Copyright Thomas Helander. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

apiVersion: v1
kind: Service
metadata:
  name: {{ template "jellyfin.jellyfin.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: jellyfin
  {{- if or .Values.services.jellyfin.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" (dict "values" (list .Values.services.jellyfin.annotations .Values.commonAnnotations) "context" .) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{ $type := .Values.services.jellyfin.type }}
  type: {{ $type }}
  {{- if and .Values.services.jellyfin.clusterIP (eq $type "ClusterIP") }}
  clusterIP: {{ .Values.services.jellyfin.clusterIP }}
  {{- end }}
  {{- if .Values.services.jellyfin.sessionAffinity }}
  sessionAffinity: {{ .Values.services.jellyfin.sessionAffinity }}
  {{- end }}
  {{- if .Values.services.jellyfin.sessionAffinityConfig }}
  sessionAffinityConfig: {{- include "common.tplvalues.render" (dict "value" .Values.services.jellyfin.sessionAffinityConfig "context" $) | nindent 4 }}
  {{- end }}
  {{- if or (eq $type "LoadBalancer") (eq $type "NodePort") }}
  externalTrafficPolicy: {{ .Values.services.jellyfin.externalTrafficPolicy | quote }}
  {{- end }}
  {{- if and (eq $type "LoadBalancer") (not (empty .Values.services.jellyfin.loadBalancerSourceRanges)) }}
  loadBalancerSourceRanges: {{ .Values.services.jellyfin.loadBalancerSourceRanges }}
  {{- end }}
  {{- if and (eq $type "LoadBalancer") (not (empty .Values.services.jellyfin.loadBalancerIP)) }}
  loadBalancerIP: {{ .Values.services.jellyfin.loadBalancerIP }}
  {{- end }}
  ports:
    {{- range .Values.services.jellyfin.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      {{- if .targetPort }}
      targetPort: {{ .targetPort }}
      {{- end }}
      protocol: {{ .protocol }}
      {{- if .nodePort }}
      nodePort: {{ .nodePort }}
      {{- else if eq $type "ClusterIP" }}
      nodePort: null
      {{- end }}
    {{- end }}
    {{- if .Values.services.jellyfin.extraPorts }}
    {{- include "common.tplvalues.render" (dict "value" .Values.services.jellyfin.extraPorts "context" $) | nindent 4 }}
    {{- end }}
  {{- $podLabels := include "common.tplvalues.merge" (dict "values" (list .Values.jellyfin.podLabels .Values.commonLabels) "context" .) | fromYaml }}
  selector: {{- include "common.labels.matchLabels" ( dict "customLabels" $podLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: jellyfin
