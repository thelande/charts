# Copyright Thomas Helander. All Rights Reserved.
# SPDX-License-Identifier: APACHE-2.0

## Please, note that this will override the image parameters, including dependencies, configured to use the global value
## Current available global Docker image parameters: imageRegistry, imagePullSecrets and storageClass
##
global:
  # -- Global Docker image registry
  imageRegistry: ""
  ## e.g:
  ## imagePullSecrets:
  ##   - myRegistryKeySecretName
  ##
  
  # -- Global Docker registry secret names as an array
  imagePullSecrets: []

  # -- Global default StorageClass for Persistent Volume(s)
  defaultStorageClass: ""

  ## Security parameters
  ##
  security:
    # -- Allows skipping image verification
    allowInsecureImages: false

## Common parameters
##
# -- Override Kubernetes version
kubeVersion: ""
# -- Override Kubernetes API versions reported by .Capabilities
apiVersions: []
# -- String to partially override common.names.name
nameOverride: ""
# -- String to fully override common.names.fullname
fullnameOverride: ""
# -- String to fully override common.names.namespace
namespaceOverride: ""
# -- Labels to add to all deployed objects
commonLabels: {}
# -- Annotations to add to all deployed objects
commonAnnotations: {}
# -- Kubernetes cluster domain name
clusterDomain: cluster.local

## Diagnostic mode
diagnosticMode:
  # -- Enable diagnostic mode (all probes will be disabled and the command will be overridden)
  enabled: false
  # -- Command to override all containers in the chart release
  command:
    - sleep
  # -- Args to override all containers in the chart release
  args:
    - infinity

jellyfin:
  image:
    # -- jellyfin image registry
    registry: docker.io
    # -- jellyfin image repository
    repository: jellyfin/jellyfin
    # -- jellyfin image tag
    tag: 10.10.7
    # -- The image pull policy
    # ref: https://kubernetes.io/docs/concepts/containers/images/#pre-pulled-images
    pullPolicy: IfNotPresent
    # -- Image pull secrets
    # Secrets must be manually created in the namespace.
    # ref: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
    pullSecrets: []
    ## e.g:
    ## pullSecrets:
    ##   - myRegistryKeySecretName
    ##
  
  # -- Number of jellyfin replicas to deploy
  replicaCount: 1

  # -- List of ports for jellyfin containers
  containerPorts:
    - name: http
      containerPort: 8096
  ## e.g:
  ## containerPorts:
  ##   - name: myservice
  ##     containerPort: 9090
  ##

  ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-probes/#configure-probes
  livenessProbe:
    # -- Enable livenessProbe on jellyfin containers
    enabled: true
    # -- Initial delay seconds for livenessProbe
    initialDelaySeconds: 0
    # -- Period seconds for livenessProbe
    periodSeconds: 10
    # -- Timeout seconds for livenessProbe
    timeoutSeconds: 1
    # -- Failure threshold for livenessProbe
    failureThreshold: 3
    # -- Success threshold for livenessProbe
    successThreshold: 1

  readinessProbe:
    # -- Enable readinessProbe on jellyfin containers
    enabled: true
    # -- Initial delay seconds for readinessProbe
    initialDelaySeconds: 0
    # -- Period seconds for readinessProbe
    periodSeconds: 10
    # -- Timeout seconds for readinessProbe
    timeoutSeconds: 1
    # -- Failure threshold for readinessProbe
    failureThreshold: 3
    # -- Success threshold for readinessProbe
    successThreshold: 1

  startupProbe:
    # -- Enable startupProbe on jellyfin containers
    enabled: false
    # -- Initial delay seconds for startupProbe
    initialDelaySeconds: 0
    # -- Period seconds for startupProbe
    periodSeconds: 10
    # -- Timeout seconds for startupProbe
    timeoutSeconds: 1
    # -- Failure threshold for startupProbe
    failureThreshold: 3
    # -- Success threshold for startupProbe
    successThreshold: 1
  
  # -- Custom livenessProbe that overrides the default one
  customLivenessProbe: {}
  # -- Custom readinessProbe that overrides the default one
  customReadinessProbe: {}
  # -- Custom startupProbe that overrides the default one
  customStartupProbe: {}
  ## jellyfin resource requests and limits
  ## ref: http://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/
  # -- Set jellyfin container resources according to one common preset (allowed values: none, nano, small, medium, large, xlarge, 2xlarge). This is ignored if jellyfin.resources is set (jellyfin.resources is recommended for production).
  # More information: https://github.com/bitnami/charts/blob/main/bitnami/common/templates/_resources.tpl#L15
  resourcesPreset: "nano"
  # -- Set jellyfin container requests and limits for different resources like CPU or memory (essential for production workloads)
  resources: {}
  ## e.g:
  ## resources:
  ##   requests:
  ##     cpu: 2
  ##     memory: 512Mi
  ##   limits:
  ##     cpu: 3
  ##     memory: 1024Mi
  ##

  ## Configure Pods Security Context
  ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/#set-the-security-context-for-a-pod
  podSecurityContext:
    # -- Enable jellyfin pods' Security Context
    enabled: true
    # -- Set filesystem group change policy for jellyfin pods
    fsGroupChangePolicy: Always
    # -- settings using the sysctl interface for jellyfin pods
    sysctls: []
    # -- Set filesystem extra groups for jellyfin pods
    supplementalGroups: []
    # -- Set fsGroup in jellyfin pods' Security Context
    fsGroup: 1001
  ## Configure Container Security Context
  ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/#set-the-security-context-for-a-container
  containerSecurityContext:
    # -- Enabled jellyfin container' Security Context
    enabled: true
    # -- Set SELinux options in jellyfin container
    seLinuxOptions: {}
    # -- Set runAsUser in jellyfin container' Security Context
    runAsUser: 1001
    runAsGroup: 1001
    # -- Set runAsNonRoot in jellyfin container' Security Context
    runAsNonRoot: true
    # -- Set readOnlyRootFilesystem in jellyfin container' Security Context
    readOnlyRootFilesystem: true
    # -- Set privileged in jellyfin container' Security Context
    privileged: false
    # -- Set allowPrivilegeEscalation in jellyfin container' Security Context
    allowPrivilegeEscalation: false
    # -- List of capabilities to be dropped in jellyfin container
    capabilities:
      drop: ["ALL"]
    # -- Set seccomp profile in jellyfin container
    seccompProfile:
      type: "RuntimeDefault"
    
  # -- Override default jellyfin container command (useful when using custom images)
  command: []
  # -- Override default jellyfin container args (useful when using custom images)
  args: []
  # -- Mount Service Account token in jellyfin pods
  automountServiceAccountToken: false
  # -- Annotations for jellyfin
  # ref: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
  annotations: {}
  # -- jellyfin pods host aliases
  # ref: https://kubernetes.io/docs/concepts/services-networking/add-entries-to-pod-etc-hosts-with-host-aliases/
  hostAliases: []

  # -- Extra labels for jellyfin pods
  # ref: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
  podLabels: {}
  # -- Annotations for jellyfin pods
  # ref: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
  podAnnotations: {}
  # -- Pod affinity preset. Ignored if `jellyfin.affinity` is set. Allowed values: `soft` or `hard`
  # ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#inter-pod-affinity-and-anti-affinity
  podAffinityPreset: ""
  # -- Pod anti-affinity preset. Ignored if `jellyfin.affinity` is set. Allowed values: `soft` or `hard`
  # ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#inter-pod-affinity-and-anti-affinity
  podAntiAffinityPreset: soft
  ## Node jellyfin.affinity preset
  ## ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity
  nodeAffinityPreset:
    # -- Node affinity preset type. Ignored if `jellyfin.affinity` is set. Allowed values: `soft` or `hard`
    type: ""
    # -- Node label key to match. Ignored if `jellyfin.affinity` is set
    key: ""
    # -- Node label values to match. Ignored if `jellyfin.affinity` is set
    values: []
    ## E.g.
    ## values:
    ##   - e2e-az1
    ##   - e2e-az2
    ##
  # -- Affinity for jellyfin pods assignment
  # ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#affinity-and-anti-affinity
  # NOTE: `jellyfin.podAffinityPreset`, `jellyfin.podAntiAffinityPreset`, and `jellyfin.nodeAffinityPreset` will be ignored when it's set
  affinity: {}
  # -- Node labels for jellyfin pods assignment
  # ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/
  nodeSelector: {}
  # --Tolerations for jellyfin pods assignment
  # ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
  tolerations: []
  # -- Statefulset strategy type
  # ref: https://kubernetes.io/docs/concepts/workloads/controllers/deployment/#strategy
  updateStrategy:
    ## ONLY FOR DEPLOYMENTS:
    ## Can be set to RollingUpdate or Recreate
    ## ONLY FOR STATEFULSETS:
    ## Can be set to RollingUpdate or OnDelete
    ##
    type: RollingUpdate
  # -- Pod management policy for jellyfin statefulset
  # ref: https://kubernetes.io/docs/concepts/workloads/controllers/statefulset/#pod-management-policies
  podManagementPolicy: OrderedReady
  # -- Seconds jellyfin pods need to terminate gracefully
  # ref: https://kubernetes.io/docs/concepts/workloads/pods/pod/#termination-of-pods
  terminationGracePeriodSeconds: ""
  # -- Array with extra environment variables to add to jellyfin containers
  extraEnvVars: []
  ## e.g:
  ## extraEnvVars:
  ##   - name: FOO
  ##     value: "bar"
  ##

  # -- Name of existing ConfigMap containing extra env vars for jellyfin containers
  extraEnvVarsCM: ""
  # -- Name of existing Secret containing extra env vars for jellyfin containers
  extraEnvVarsSecret: ""
  # -- Optionally specify extra list of additional volumes for the jellyfin pods
  extraVolumes: []
  # -- Optionally specify extra list of additional volumeMounts for the jellyfin containers
  extraVolumeMounts: []
  # -- Add additional sidecar containers to the jellyfin pods
  sidecars: []
  ## e.g:
  ## sidecars:
  ##   - name: your-image-name
  ##     image: your-image
  ##     imagePullPolicy: Always
  ##     ports:
  ##       - name: portname
  ##         containerPort: 1234
  ##

  # -- Add additional init containers to the jellyfin pods
  # ref: https://kubernetes.io/docs/concepts/workloads/pods/init-containers/
  initContainers: []
  ## e.g:
  ## initContainers:
  ##  - name: your-image-name
  ##    image: your-image
  ##    imagePullPolicy: Always
  ##    command: ['sh', '-c', 'echo "hello world"']
  ##
  persistence:
    # -- Enable persistence using Persistent Volume Claims
    enabled: false

services:
  jellyfin:
    # -- The service type
    type: ClusterIP
    
    ports:
      - name: http
        port: 8096
        protocol: TCP
    # -- jellyfin service Cluster IP
    clusterIP: ""
    ## e.g.:
    ## clusterIP: None

    # -- jellyfin service Load Balancer IP
    # ref: https://kubernetes.io/docs/concepts/services-networking/service/#type-loadbalancer
    loadBalancerIP: ""
    # -- jellyfin service Load Balancer sources
    # ref: https://kubernetes.io/docs/tasks/access-application-cluster/configure-cloud-provider-firewall/#restrict-access-for-loadbalancer-service
    loadBalancerSourceRanges: []
    ## e.g:
    ## loadBalancerSourceRanges:
    ##   - 10.10.10.0/24
    ##

    # -- jellyfin service external traffic policy
    # ref http://kubernetes.io/docs/tasks/access-application-cluster/create-external-load-balancer/#preserving-the-client-source-ip
    externalTrafficPolicy: Cluster
    # -- Additional custom annotations for jellyfin service
    annotations: {}
    # -- Extra ports to expose in jellyfin service (normally used with the `sidecars` value)
    extraPorts: []
    # -- Control where client requests go, to the same pod or round-robin
    # Values: ClientIP or None
    # ref: https://kubernetes.io/docs/concepts/services-networking/service/
    sessionAffinity: None
    # -- Additional settings for the sessionAffinity
    sessionAffinityConfig: {}
    ## e.g:
    ## sessionAffinityConfig:
    ##   clientIP:
    ##     timeoutSeconds: 300
    ##

ingress:
  # -- jellyfin ingress parameters
  # ref: http://kubernetes.io/docs/concepts/services-networking/ingress/
  jellyfin:
    # -- Enable ingress record generation for jellyfin
    enabled: false
    # -- Ingress path type
    pathType: ImplementationSpecific
    # -- Force Ingress API version (automatically detected if not set)
    apiVersion: ""
    # -- Default host for the ingress record
    hostname: jellyfin.local
    # -- IngressClass that will be be used to implement the Ingress (Kubernetes 1.18+)
    # This is supported in Kubernetes 1.18+ and required if you have more than one IngressClass marked as the default for your cluster .
    # ref: https://kubernetes.io/blog/2020/04/02/improvements-to-the-ingress-api-in-kubernetes-1.18/
    ingressClassName: ""
    # -- Default path for the ingress record
    # NOTE: You may need to set this to '/*' in order to use this with ALB ingress controllers
    path: /
    # -- Additional annotations for the Ingress resource. To enable certificate autogeneration, place here your cert-manager annotations.
    # Use this parameter to set the required annotations for cert-manager, see
    # ref: https://cert-manager.io/docs/usage/ingress/#supported-annotations
    annotations: {}
    ## e.g:
    ## annotations:
    ##   kubernetes.io/ingress.class: nginx
    ##   cert-manager.io/cluster-issuer: cluster-issuer-name
    ##

    # -- Enable TLS configuration for the host defined at `ingress.hostname` parameter
    # TLS certificates will be retrieved from a TLS secret with name: `{{- printf "%s-tls" .Values.ingress.hostname }}`
    # You can:
    #   - Use the `ingress.secrets` parameter to create this TLS secret
    #   - Rely on cert-manager to create it by setting the corresponding annotations
    #   - Rely on Helm to create self-signed certificates by setting `ingress.selfSigned=true`
    tls: false
    # -- Create a TLS secret for this ingress record using self-signed certificates generated by Helm
    selfSigned: false
    # -- An array with additional hostname(s) to be covered with the ingress record
    extraHosts: []
    ## e.g:
    ## extraHosts:
    ##   - name: [[ main_object_block.component ]].local
    ##     path: /
    ##

    # -- An array with additional arbitrary paths that may need to be added to the ingress under the main host
    extraPaths: []
    ## e.g:
    ## extraPaths:
    ## - path: /*
    ##   backend:
    ##     serviceName: ssl-redirect
    ##     servicePort: use-annotation
    ##

    # -- TLS configuration for additional hostname(s) to be covered with this ingress record
    # ref: https://kubernetes.io/docs/concepts/services-networking/ingress/#tls
    extraTls: []
    ## e.g:
    ## extraTls:
    ## - hosts:
    ##     - [[ main_object_block.component ]].local
    ##   secretName: [[ main_object_block.component ]].local-tls
    ##

    # -- Custom TLS certificates as secrets
    # NOTE: 'key' and 'certificate' are expected in PEM format
    # NOTE: 'name' should line up with a 'secretName' set further up
    secrets: []
    ## If it is not set and you're using cert-manager, this is unneeded, as it will create a secret for you with valid certificates
    ## If it is not set and you're NOT using cert-manager either, self-signed certificates will be created valid for 365 days
    ## It is also possible to create and manage the certificates outside of this helm chart
    ## Please see README.md for more information
    ## e.g:
    ## secrets:
    ##   - name: [[ main_object_block.component ]].local-tls
    ##     key: |-
    ##       -----BEGIN RSA PRIVATE KEY-----
    ##       ...
    ##       -----END RSA PRIVATE KEY-----
    ##     certificate: |-
    ##       -----BEGIN CERTIFICATE-----
    ##       ...
    ##       -----END CERTIFICATE-----
    ##

    # -- Additional rules to be covered with this ingress record
    # ref: https://kubernetes.io/docs/concepts/services-networking/ingress/#ingress-rules
    extraRules: []
    ## e.g:
    ## extraRules:
    ## - host: example.local
    ##     http:
    ##       path: /
    ##       backend:
    ##         service:
    ##           name: example-svc
    ##           port:
    ##             name: http
    ##


persistence:
  config:
    # -- Enable persistence using Persistent Volume Claims
    enabled: false
    # -- Path to mount the volume at.
    mountPath: /data/jellyfin-config
    # -- The subdirectory of the volume to mount to, useful in dev environments and one PV for multiple services
    subPath: ""
    # -- Storage class of backing PVC
    # If defined, storageClassName: <storageClass>
    # If set to "-", storageClassName: "", which disables dynamic provisioning
    # If undefined (the default) or set to null, no storageClassName spec is
    #   set, choosing the default provisioner.  (gp2 on AWS, standard on
    #   GKE, AWS & OpenStack)
    storageClass: ""
    # -- Persistent Volume Claim annotations
    annotations: {}
    # -- Persistent Volume Access Modes
    accessModes:
      - ReadWriteOnce
    # -- Size of data volume
    size: 8Gi
    # -- The name of an existing PVC to use for persistence
    existingClaim: ""
    # -- Selector to match an existing Persistent Volume for WordPress data PVC
    # If set, the PVC can't have a PV dynamically provisioned for it
    selector: {}
    ## e.g.
    ## selector:
    ##   matchLabels:
    ##     app: my-app
    ##

    # -- Custom PVC data source
    dataSource: {}
  cache:
    # -- Enable persistence using Persistent Volume Claims
    enabled: false
    # -- Path to mount the volume at.
    mountPath: /data/jellyfin-cache
    # -- The subdirectory of the volume to mount to, useful in dev environments and one PV for multiple services
    subPath: ""
    # -- Storage class of backing PVC
    # If defined, storageClassName: <storageClass>
    # If set to "-", storageClassName: "", which disables dynamic provisioning
    # If undefined (the default) or set to null, no storageClassName spec is
    #   set, choosing the default provisioner.  (gp2 on AWS, standard on
    #   GKE, AWS & OpenStack)
    storageClass: ""
    # -- Persistent Volume Claim annotations
    annotations: {}
    # -- Persistent Volume Access Modes
    accessModes:
      - ReadWriteOnce
    # -- Size of data volume
    size: 8Gi
    # -- The name of an existing PVC to use for persistence
    existingClaim: ""
    # -- Selector to match an existing Persistent Volume for WordPress data PVC
    # If set, the PVC can't have a PV dynamically provisioned for it
    selector: {}
    ## e.g.
    ## selector:
    ##   matchLabels:
    ##     app: my-app
    ##

    # -- Custom PVC data source
    dataSource: {}

## ServiceAccount configuration
##
serviceAccount:
  # -- Specifies whether a ServiceAccount should be created
  create: true
  # -- The name of the ServiceAccount to use.
  # If not set and create is true, a name is generated using the common.names.fullname template
  name: ""
  # -- Additional Service Account annotations (evaluated as a template)
  annotations: {}
  # -- Automount service account token for the server service account
  automountServiceAccountToken: true
  

